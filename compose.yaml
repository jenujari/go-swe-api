services:
  test_sweapi:
    image: test_sweapi:latest
    build:
      dockerfile: ./test.Dockerfile
      # args:
      #   - GH_USER=${GH_USER}
      #   - GH_TOKEN=${GH_TOKEN}
    volumes:
      - ./:/src
    working_dir: /src
  sweapi:
    image: swegrpc:latest
    build:
      dockerfile: ./Dockerfile
    ports:
      - "5678:5678"
